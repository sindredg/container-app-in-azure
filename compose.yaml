services:
  api:
    build: ./app/api
    environment:
      SERVICE_VERSION: "0.1.0-local"

  web:
    build: ./app/web
    ports:
      - "8080:8080"
    environment:
      API_UPSTREAM: http://api:8080
    depends_on:
      api:
        condition: service_healthy